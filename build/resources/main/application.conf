ktor {
    deployment {
        port = 8006
        port = ${?PORT}
    }
    application {
        modules = [ com.enestekin.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8006"
    audience = "main"
    secret = "secret"
    realm = "social network"
}
