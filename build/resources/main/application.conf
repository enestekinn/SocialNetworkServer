ktor {
    deployment {
        port = 8007
        port = ${?PORT}
    }
    application {
        modules = [ com.enestekin.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8007"
    audience = "main"
    secret = "secret"
    realm = "social network"
}
