ktor {
    deployment {
        port = 8008
        port = ${?PORT}
    }
    application {
        modules = [ com.enestekin.ApplicationKt.module ]
    }
}
jwt {
    domain = "http://0.0.0.0:8008"
    audience = "main"
    secret = "secret"
    realm = "social network"
}
